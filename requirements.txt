#  Core 
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-dotenv>=1.0.0
numpy>=1.26.0
pydantic>=2.0.0
requests>=2.32.0

#  Embeddings (local  no API key required) 
sentence-transformers>=3.0.0
torch>=2.0.0

#  Database 
psycopg2-binary>=2.9.9
pgvector>=0.3.0

#  LLM Providers  install the one(s) you use 
cerebras-cloud-sdk>=1.0.0       # LLM_PROVIDER=cerebras  (default)
# openai>=1.0.0                 # LLM_PROVIDER=openai
# anthropic>=0.20.0             # LLM_PROVIDER=anthropic

#  Optional 
# redis>=5.0.0                  # ENABLE_CACHE=true -> Redis intent/embedding cache
